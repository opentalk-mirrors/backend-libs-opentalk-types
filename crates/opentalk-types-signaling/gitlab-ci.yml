# SPDX-FileCopyrightText: OpenTalk GmbH <mail@opentalk.eu>
#
# SPDX-License-Identifier: EUPL-1.2

---

test:opentalk-types-signaling:
  extends: .test:types
  variables:
    PACKAGE: opentalk-types-signaling
  parallel:
    matrix:
      - FEATURES:
          [
          '',
            backend,
            frontend,
            rand,
            redis,
            serde,
          ]
        EXTRA_ARGS: ['', --no-default-features]
